<h2>Import from CSV</h2>


<%= form_tag import_appreciations_path, multipart: true,
                    class: "nifty_form" do %>

<%= label_tag(:category_id, "Selectionnez une catégorie")%>
<%= select_tag(:category_id, options_from_collection_for_select(Category.all, :id, :title), :include_blank => false)%> <br>
  <%= file_field_tag :file %><br>
  <%= submit_tag "Import" , class: "btn btn-large btn-primary" %>
<% end %>