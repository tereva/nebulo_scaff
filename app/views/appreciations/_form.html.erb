<%= form_for(@appreciation) do |f| %>
  <% if @appreciation.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@appreciation.errors.count, "error") %> prohibited this appreciation from being saved:</h2>

      <ul>
      <% @appreciation.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :content %><br>
    <%= f.text_area :content %>
  </div>
  <div class="field">
    <%= f.label :remark %><br>
    <%= f.text_field :remark %>
  </div>
  <div class="field">
    <%= f.label :category_id %><br>
<%= f.select(:category_id , options_from_collection_for_select(Category.all, :id, :title)) %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
