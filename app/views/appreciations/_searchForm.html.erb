

<div class="row">
	<div class="col-md-6 col-md-offset-3"> 

		<div class="panel panel-primary panelrow">
			<div class="panel-heading">
				Choisissez deux mots 
			</div>

			<div class="panel-body">

				<%= form_tag search_path, class: "nifty_form" do %>
				<div class="form-group">

					<%= label_tag(:kw1, "Selectionnez un premier mot")%>
					<%= select_tag(:kw1, options_from_collection_for_select(@keyword_list, :id, :word), :include_blank => true)%>
				</div>

				<div class="form-group">
					<%= label_tag(:kw2, "Selectionnez un deuxième mot")%>
					<%= select_tag(:kw2, options_from_collection_for_select(@keyword_list, :id, :word), :include_blank => true)%>

				</div>

				<%= hidden_field_tag(:cat, @cat) %>

				<%= submit_tag "Lancer la recherche", class: "btn btn-large btn-primary"  %>

				<% end %>
			</div>

		</div>
	</div> <!-- col md6 -->
</div> <!-- row -->

